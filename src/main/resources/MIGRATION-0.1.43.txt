VibePetCore 0.1.43

- Исправлен краш `Пет-Мастера` при `/vpc master set` и `/vpc master respawn`.
- Причина была в том, что Paper не принимает русские символы во внутреннем имени `PlayerProfile`.
- Теперь skin-head мастера создаётся через безопасное ASCII-имя и может нормально спавниться.
