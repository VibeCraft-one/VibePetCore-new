# VibePetCore Context

## Release Snapshot
- release triage/backlog: `RELEASE_BUGHUNT_PLAN.md`
- role prompts для следующих чатов: `RELEASE_TASK_PROMPTS.md`
- очередь исполнения: `RELEASE_EXECUTION_QUEUE.md`
- launch-файл чатов: `RELEASE_CHAT_LAUNCH.md`
- smoke доказательство по живому GUI/Source: `SMOKE-2.6.26.txt`
- короткий ручной релизный pack для тестеров: `MANUAL_STACK_GATE-2.6.31.txt`
- честная оценка готовности на `2026-06-24`: `~92%`, релиз ещё `не готов`
- закрыты кодом и тестами: `VPC-REL-BUGHUNT-QUEST-SAVE-01`, `VPC-REL-BUGHUNT-EVOLUTION-SAVE-01`, `VPC-REL-BUGHUNT-FORGE-SAVE-01`, `VPC-REL-BUGHUNT-CORE-REPAIR-SAVE-01`
- живым smoke уже подтверждены: `Source offhand conflict`, `quest accept/turn-in`, `forge spend`, `box spend`, `actual evolution button`, `core repair`
- код-аудит hot-path не выявил нового очевидного TPS-blocker в `TaskManager`, `PetInterestLocator`, `PetAbilityService`, `PetMasterManager`, `LootBoxManager`
- ближайший обязательный проход: `VPC-REL-STACK-MANUAL-01`

## Что это
`VibePetCore` — Paper/Purpur-плагин про питомцев-спутников.
Основа: ядро питомца в `offhand`, призыв по `ПКМ`, runtime-пет рядом с игроком, кормление, эволюция, боевые и utility-реакции, GUI, квесты и очки.

## Ключевые файлы
- `src/main/java/dev/li2fox/vibepetcore/core/VibePetCorePlugin.java`
- `src/main/java/dev/li2fox/vibepetcore/egg/PetEggController.java`
- `src/main/java/dev/li2fox/vibepetcore/egg/PetEggService.java`
- `src/main/java/dev/li2fox/vibepetcore/pet/PetEngineManager.java`
- `src/main/java/dev/li2fox/vibepetcore/pet/RuntimePet.java`
- `src/main/java/dev/li2fox/vibepetcore/pet/ability/PetAbilityService.java`
- `src/main/java/dev/li2fox/vibepetcore/gui/PetGuiService.java`
- `src/main/java/dev/li2fox/vibepetcore/player/PlayerDataManager.java`

## Текущая механика
- Источник правды по активному питомцу: `PlayerData.activePetId` + `OwnedPetData`.
- Простое перекладывание ядра во вторую руку не призывает пета само по себе.
- Призыв: ядро во вторую руку -> `ПКМ` -> отсчёт -> спавн.
- Первый призыв отдельный: 3 секунды, обычный призыв: 2 секунды; на первом призыве перед игроком идут золотые искры.
- Если питомец активен и его ядро во второй руке, предмет меняется на маленькую зачарованную кнопку.
- Если ядро убрано из второй руки, runtime-пет скрывается, но данные игрока остаются целыми.

## Визуальные договорённости
- Над питомцем: коротко, например `Лиса [Э2 Ур.4]`.
- Action bar: коротко, например `[HP 18/20 Еда 4/5]`.
- Лор яйца:
  - без устаревающего таймера;
  - конкретные бонусы игроку;
  - штраф за смерть показывается только пока реально активен.
- Ранние формы должны выглядеть малышами; рост по эволюциям заметный.

## Нюансы и слабые места
- Самая хрупкая зона: синхронизация `PlayerData <-> предмет <-> runtime pet`.
- Проверять сценарии:
  `join`, `respawn`, `world change`, `creative/survival`, перекладывание предмета, рестарт сервера.
- Старые яйца прошлых версий могут вести себя странно; для зачистки есть `DANGER-DELETE`.
- Коты и волки теперь запоминают внешний вид через `OwnedPetData.progress()`.

## Админка
- `/vpc DANGER-DELETE All`
- `/vpc DANGER-DELETE <player>`
- После ввода админ должен подтвердить действие словом `yes` в течение 10 секунд.
- `All` поднимает `egg-core.purge-epoch`, чтобы старые яйца прошлых поколений стали невалидны.

## Repo hygiene
- `src/main/resources/config.yml` должен хранить только placeholder-значения для MySQL; реальные DB-данные живут только в серверном локальном конфиге.
- DB-пароль, который уже попадал в GitHub до 2026-06-21, считать скомпрометированным и заменить на сервере.

## GUI refactor
- Текущий безопасный курс: одна самостоятельная GUI-страница = отдельный `*Page` класс.
- `PetGuiService` пока остаётся listener/facade, чтобы не ломать команды, события и cooldown guards.
- Уже вынесены: `SourceMainPage`, `SourceQuestPage`, `SourceBoxPage`, `SourceForgePage`, `SourceLegendaryPage`, `SourceHelpPage`, `PetArmorHelpPage`, `GrowthMissingPage`, `PetInfoPage`, `PetOverviewPage`.
- `PetOverviewPage` содержит render, click-handler и item-builder'ы личного `/pet` экрана; `PetGuiService` держит действия/синхронизацию.
- `SourceQuestPage` содержит render/click-handler квестов Источника, категории и пагинацию; `quests:*` маршрутизируется через `PetGuiRouter`.
- `PetGuiRouter` и `GuiPageId` должны постепенно забирать новые страницы и клики.
- Не переносить все страницы одним большим патчем; после каждой страницы запускать `compileJava test`.

## Последние важные изменения
- `2.6.26`
  - ремонт ядра теперь требует успешного `playerData save` после расхода тотема и изменения durability/vitals;
  - при save-fail тотем и состояние питомца откатываются, ложный success не показывается;
  - добавлен regression-тест на rollback-контракт ремонта ядра без Bukkit server.
- `2.6.27`
  - personal `/pet` flow больше не подменяет active/offhand core случайным `main hand` ядром при выборе repair/growth/quest context;
  - `repair` и `syncOffhandEgg()` теперь держатся за matching active-core в `offhand`, поэтому чужое ядро в `main hand` не уводит клик и не оставляет stale active-button state;
  - добавлен unit-тест на выбор правильного ядра для personal menu.
- `2.6.28`
  - `/pet help summon` и pet-type help больше не врут про main-hand summon и больше не обещают одинаковые таймеры призыва;
  - shipped help теперь честно говорит: filled/matching empty core в `offhand`, первый summon `3 сек`, обычный `2 сек`;
  - добавлен regression-тест на help summon truthfulness.
- `2.6.29`
  - recall пустым ядром больше не принимает другое ядро того же pet-type: теперь для возврата нужен тот же `petId` или unreadable legacy core;
  - это закрывает риск потери второго питомца, когда активный pet мог перезаписать чужое empty core того же типа;
  - добавлен unit-тест на recall target matching.
- `2.6.30`
  - Source/Forge GUI больше не подменяет целевое ядро случайным `main hand` core, если активное или ожидаемое ядро уже в `offhand`;
  - это закрывает риск, когда кузня могла показать и улучшать не то ядро и сжигать donor eggs не по ожидаемому active/offhand core;
  - helper выбора GUI-ядра сделан общим для personal `/pet` и Source/Forge, добавлен regression-тест на offhand приоритет.
- `2.6.31`
  - Source/Quests GUI больше не берёт стадию и выбранного питомца из случайного `main hand` ядра, если правильное ядро уже в `offhand`;
  - это закрывает риск ложной блокировки `evolution` квеста и неправильного статуса/стадии в Source quest GUI;
  - добавлен regression-тест на offhand приоритет для quest GUI.
- `2.6.25`
  - кузня ядра теперь требует успешного `playerData save` после расхода donor eggs и попытки апгрейда редкости;
  - при save-fail donor eggs и rarity/state активного питомца откатываются, ложный success не показывается;
  - добавлен regression-тест на rollback-контракт кузни без Bukkit server.
- `2.6.24`
  - эволюция активного питомца теперь требует успешного `playerData save` после расхода ресурсов и попытки stage-up;
  - при save-fail откатываются evolution materials из инвентаря и рюкзака питомца, stage/level/bond/xp восстанавливаются, ложный success не показывается;
  - добавлен regression-тест на rollback-контракт эволюции без Bukkit server.
- `2.6.23`
  - evolution-items больше не запускают эволюцию через обычное right-click кормление питомца;
  - клик по питомцу `NETHER_STAR`/`ECHO_SHARD`/`DIAMOND` теперь даёт короткую подсказку про `/pet` или Источник и не списывает предмет;
  - эволюция остаётся в Source/GUI/command flow с проверкой ресурсов, квестов стадии и chance.
- `2.6.22`
  - sync активного питомца больше не превращает ядро во второй руке в active-button, если повторный `activatePet()` не смог сохраниться;
  - missing-offhand cleanup теперь показывает игроку понятное `pet-egg.save-failed` сообщение при сбое сохранения, с коротким cooldown от спама.
- `2.6.21`
  - открытие базовой коробки/Источника теперь сохраняет стоимость, free cooldown и pity до выдачи яйца;
  - если save player-data падает, состояние открытия откатывается в памяти, яйцо не выдаётся, игрок видит понятное сообщение повторить позже;
  - добавлен regression-тест на rollback-контракт открытия коробки без Bukkit server.
- `2.6.20`
  - активация питомца теперь считает save активного состояния обязательным: без успешного save предмет не меняется на active-button/empty-core;
  - при save-fail во время активации откатываются `activePetId`, in-memory pet data и runtime-spawn, игрок видит понятное сообщение повторить позже;
  - добавлен regression-тест на rollback-контракт без Bukkit server.
- `2.6.19`
  - `PetEggController.syncPlayer()` больше не поднимает и не удерживает runtime-питомца по ядру из обычного слота инвентаря;
  - активный runtime требует matching core/active-button в offhand slot 40, иначе runtime скрывается и `activePetId` очищается через обычный save-flow;
  - при `egg-core.offhand-activation=true` призыв из main hand блокируется понятным actionbar, GUI-тексты больше не обещают main hand / either hand.
- `2.6.18`
  - `/pet` overview и `TEST_CHECKLIST.md` больше не обещают несуществующую GUI-кнопку режима движения;
  - тексты позиционного контроллера уточняют реальное поведение: стекла выбирают позицию 0-7, `Ближе`/`Дальше` меняют дистанцию;
  - логика движения не менялась: это выравнивание GUI/чеклиста с текущим runtime-поведением.
- `2.6.17`
  - обзорная карточка эволюции в `/pet` больше не показывает термин "связь": связь осталась требованием и отображается на детальном экране эволюции;
  - `TEST_CHECKLIST.md` больше не ведёт тестера через legacy `/vpc master set`, основная команда Источника: `/vpc source set`;
  - добавлен regression-тест на компактность overview-текстов и отсутствие старого Пет-Мастера в чеклисте.
- `2.6.16`
  - follow/teleport fallback больше не прыгает сразу в `owner.getLocation()`: перед этим проверяются дополнительные safe-точки вокруг игрока;
  - stuck-трекинг теперь считает боковой обход препятствия полезным движением и не запускает ложный recovery, если питомец не уходит явно дальше от владельца;
  - добавлен unit-тест на reset-логику stuck tracking.
- `2.6.15`
  - comfort-rest больше не вызывает `nearestEnemy()` на каждом pet-update tick;
  - проверка угроз для отдыхающего питомца throttled примерно до 0.85-1.2 сек с jitter по petId, чтобы не создавать nearbyEntities bursts;
  - добавлен unit-тест на стабильный диапазон delay threat-check.
- `2.6.14`
  - runtime-пит больше не применяет static entity state каждый update: owner-collision exemption, empty hands, persistent appearance и scale кэшируются по entity/type/evolution;
  - custom name питомца обновляется только при смене petName/type/rarity/evolution/level, а не на каждом pet-update tick;
  - social scan питомцев получил budget + rotation: малый онлайн сканируется полностью, большой онлайн не уходит в квадратичный O(n²) scan каждую секунду.
- `2.6.13`
  - `/vpc reload` теперь перезапускает модули безопасным порядком: сначала все `disable` в обратном порядке, затем все `enable` в исходном;
  - это убирает риск reload-рассинхрона, где `PlayerDataManager` уже очищен/перезагружен, а `PetEngineManager` ещё держит runtime-питов;
  - добавлен unit-тест на порядок reload модулей.
- `2.6.12`
  - возврат активного питомца в ядро теперь сначала подтверждает сохранение `activePetId=null`, и только потом меняет предмет игрока;
  - `drop`, `quit`, creative/spectator recall и смерть больше не должны оставлять рассинхрон `предмет одно, PlayerData другое` при сбое SQLite/MySQL;
  - при ошибке сохранения core-state остаётся прежним, пишется throttled warning, а death-flow дополнительно despawn'ит runtime-питомца.
- `2.6.11`
  - сдача квеста теперь начисляет полную configured-награду Pet Points и не режется общим anti-abuse minute cap;
  - заголовки evolution-квестов в GUI используют выбранного/активного питомца при проверке готовности;
  - GUI коробки Источника показывает резервные попытки и честно пишет, что открытие может тратить free cooldown, очки или резерв.
- `2.6.10`
  - `/pet` active-only кнопки теперь честно показывают, что нужен призванный питомец: позиция, дистанция, рюкзак, автолут, защита, пассивки;
  - экран эволюции больше не позволяет нажатием на held-core случайно попытаться эволюционировать другого активного питомца;
  - клик по рюкзаку без активного питомца теперь даёт явное сообщение, а не молчит.
- `2.6.9`
  - legacy-алтарь Источника снова открывается даже с ядром питомца в руке и не запускает параллельный призыв;
  - `/pet help` возвращает игрока в личное меню питомца, а не в hub Источника;
  - броня питомца выровнена по этапам: copper E1, iron E2, gold E3, diamond E4, netherite E5;
  - рюкзак питомца выбирает strongest allowed armor, а лишнюю/слишком высокую броню возвращает игроку;
  - тексты GUI/квестов приведены к "Источник" вместо видимого "Пет-Мастер".
- `2.6.8`
  - кормление больше не лечит HP напрямую: еда пополняет сытость, а обычное восстановление HP идёт через satiety regen;
  - обычная еда не тратится, если сытость уже полная и другого эффекта нет;
  - feedback кормления переведён в action bar, успешное кормление даёт лёгкие зелёные частицы.
- `2.6.7`
  - активный питомец снова переводит ядро во второй руке в active-button, а не в обычное пустое ядро;
  - ПКМ по Источнику питомцев с ядром в руке не запускает призыв питомца параллельно с GUI Источника;
  - открытие Источника/лутбокса защищено коротким service-layer guard от дублей через GUI/entity события.
- `0.1.60`
  - обычный беговой hop и obstacle-hop наземных питомцев зажаты до низкой дуги;
  - прыжок через 1 блок больше не должен подкидывать Э1 на 3-5 блоков вверх.
- `0.1.59`
  - obstacle-hop наземных питомцев снижен с катапультного `1.10` до нормального диапазона по типам;
  - малыши/ур.1 прыгают ниже, но исправленное сглаживание вертикали сохранено.
- `0.1.58`
  - help питомцев показывает конкретные бафы по эволюциям вместо абстрактной роли;
  - Luck/Удача убрана из пассивных бафов и описаний.
- `0.1.57`
  - урон питомцев получил ролевые множители по типам;
  - пассивные эффекты переведены на каст-окна, cooldown и лимит эффектов по эволюции.
- `0.1.56`
  - наземные питомцы получили высокий obstacle-hop около `1.10`;
  - сглаживание скорости больше не съедает вертикаль прыжка, особенно заметно на Э1.
- `0.1.55`
  - эволюция переведена на явные требования по этапам `progression.evolution.stages`;
  - Э2/Э3/Э4/Э5 теперь имеют разные пороги уровня, связи и заданий стадии;
  - GUI эволюции и `/pet help growth` показывают реальные требования из баланса.
- `0.1.21`
  - честный и компактный лор яйца;
  - первый призыв оформлен как отдельное событие;
  - стартовые размеры большинства петов сильно уменьшены;
  - коты и волки помнят внешний вид;
  - у всех петов есть стартовый "Рюкзак питомца";
  - все петы могут минимально защищать игрока, боевые делают это чаще.
- `0.1.20`
  - `DANGER-DELETE` с подтверждением;
  - `CODEX_CONTEXT.md` как быстрый вход в проект.
- `0.1.18-0.1.19`
  - призыв по `ПКМ + 3..2..1`;
  - кнопка во второй руке вместо полного яйца;
  - сжатый UI над питомцем и в action bar.

## Правило для следующих правок
- При заметных изменениях обновляй этот файл коротко.
- Поднимай версию в `build.gradle.kts`, если меняется поведение плагина.
- Если этап заметный для сервера/игроков, добавляй новый `MIGRATION-*.txt`.
- Для координации релизного bug hunt смотри `RELEASE_BUGHUNT_PLAN.md`.
- Готовые prompts для следующих coordinator/builder/reviewer проходов лежат в `RELEASE_TASK_PROMPTS.md`.

## Анти-Моjibake
- Русские файлы правь и читай как UTF-8 без BOM.
- Если PowerShell показывает кракозябры, перепроверь `Get-Content -Encoding UTF8`, а не содержимое репозитория.
- При правках с русским текстом сначала меняй один файл, потом сразу проверяй diff и компиляцию, чтобы не разнести encoding по проекту.
